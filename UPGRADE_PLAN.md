# VRChat IL2CPP 逆向 — Unity 6 升级计划书

> 目标:在 Unity 6 (6000.0.60f1) 基线上,把反混淆质量推到一个更高且**可验证**的水平。
> 原则:每一步都基于实测数据,有明确验收标准,不靠估计。

---

## 一、当前真实状态(2026-06-29 实测)

| 维度 | 总量 | 已语义命名 | 覆盖率 | 剩余缺口 |
|---|---|---|---|---|
| 类 | 64,773 | 60.3% | — | 9,465 个混淆类仍是 fallback 名 |
| 方法 | 569,859 | 93.5% | — | 36,873 个仍是 hash (6.5%) |
| 字段 | 188,384 | 82.7% | — | 32,543 个仍是 f_XXX (17.3%) |

**结构破解已全部完成**(本 session):MethodInfo→klass@+0x20、类名@+0x98、FieldInfo stride 0x20/name@+0x08/parent@+0x18、parent@+0xA0。提取器带三层自适应(ASLR band 探测 / Transform 自检 / parent 共识),跨 dump 验证稳定。

### 最大机会点(实测,非估计)

剩余 9,465 个未命名混淆类中:
- **7,118 个有可读字段/方法签名 → 可命名**(本 session 只完成 430,占 6%)
- 1,601 个弱信号(1 个可读)
- 746 个零信号(全 f_/m_,放弃)

7,118 个的信号分层:
- **860 个强信号**(≥4 语义字段)— 命名准确率最高
- 1,021 个中信号(2-3 字段)
- 5,237 个靠方法签名

全部位于 global 命名空间(混淆器剥离了 namespace)。

---

## 二、升级目标(可量化验收)

| 维度 | 现在 | 目标 | 验收方式 |
|---|---|---|---|
| 类语义覆盖 | 60.3% | **≥ 72%** | coverage_stats.json semantic_pct |
| 字段语义覆盖 | 82.7% | **≥ 90%** | 同上,用 cross-version 字段类型补全 |
| 方法语义覆盖 | 93.5% | **≥ 95%** | cross-version 方法名回填 |
| 命名正确性 | 未系统校验 | **抽样 ≥ 90% 合理** | 独立 agent 交叉审计 |

---

## 三、分阶段计划(脚踏实地,每阶段可独立验收)

### Stage A — 强/中信号类命名(7,118 个)【最高 ROI】
**做什么**:分片并行,据字段签名推断语义类名。本 session 已验证此法有效(430 条,质量高)。
- A1:860 强信号 — 11 片 × 80,sonnet 并行,预期 ≥85% 命中
- A2:1,021 中信号 — 13 片并行
- A3:5,237 方法签名类 — 分批,需结合 method 语义,命中率较低但量大

**关键工程纪律**(本 session 踩过的坑):
- agent 一律 **Write 写文件**,只回报条数 — 避免 32k 输出上限截断
- 每片独立 ground-truth 提示 + 跳过低信号,不瞎猜
- 汇总后**去重 + 重名加后缀 + 原名校验**(必须存在于目标集)

**验收**:类语义覆盖 60.3% → ≥72%,且抽样 50 个人工/agent 复核 ≥90% 合理。

### Stage B — 字段类型 + 字段名补全
**做什么**:32,543 个 f_XXX 字段。
- B1:用 `cross_version_method_names.json`(40,223 条)同源思路,建 cross-version **字段**映射,把旧 build 已知字段名回填到结构相同的类
- B2:已命名类的字段,用类语义 + 字段 type 指针(FieldInfo+0x00 的 Il2CppType*)反推类型,辅助命名

**前置**:需要解析 FieldInfo.type 的 Il2CppType 编码(本 session 只取了名字,type 指针还没解)。这是一个**新的小破解任务**。

**验收**:字段语义覆盖 82.7% → ≥90%。

### Stage C — 方法名 cross-version 回填
**做什么**:36,873 个 hash 方法,用 method_pointer / 调用图 / cross-version 名库匹配回填。基础设施已存在(callgraph_method_names、string_api 等)。
**验收**:方法 93.5% → ≥95%。

### Stage D — 全量一致性审计【质量保证】
**做什么**:对所有 semantic_source=workflow 的命名,独立 agent 抽样交叉审计,产出错误清单并回滚明显错误。
**验收**:抽样合理率 ≥90%,错误命名回滚。

### Stage E — 收尾固化
- 清理 scratch 文件、补全 gitignore
- 重跑完整 pipeline 生成最终 deobfuscated_dump.cs + 源码树
- push 到 origin(本地领先 5 提交,有丢失风险)
- 更新 cross-version 名库供下个 build 复用

---

## 四、风险与纪律(基于本 session 实际教训)

1. **机器稳定性**:这台机器有第三方内核驱动冲突史(KMODE 蓝屏)。所有 Stage **纯计算、不 mmap 大 dump**,内存占用低。除非要取新 dump,否则不碰大内存操作。dump 后立即 taskkill 释放。
2. **不丢成果**:每个 Stage 完成立即 commit(无 AI 署名);阶段间 push。
3. **不撒 agent 在错的地方**:结构破解是串行(已完成);命名/审计才 fan-out。
4. **Workflow DSL 工具在本沙箱不可用**(模块格式报矛盾错)→ 用多 Agent 并行调用替代,效果等同。
5. **诚实验收**:每个 Stage 用 coverage_stats 实测,不靠估计报数。

---

## 五、执行顺序建议

```
Stage A (类命名, 最高 ROI) → commit → push
  ├─ A1 强信号 860   [立即可做,1 轮并行]
  ├─ A2 中信号 1021  [1 轮并行]
  └─ A3 方法签名 5237 [多轮]
Stage B (字段, 需先破 Il2CppType 编码) → commit
Stage C (方法回填) → commit
Stage D (审计) → commit
Stage E (固化 + push)
```

**建议先做 Stage A1**(860 强信号):一轮并行即可见效,验证流水线后再放大到 A2/A3。
